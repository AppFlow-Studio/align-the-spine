/** shadcn/Aceternity UI registry components import `cn` from "@/lib/utils"
 * by convention — re-exported from lib/cn.ts (this project's existing,
 * single source of truth for cn) so pasted-in components resolve without
 * edits. Prefer "@/lib/cn" for code you write yourself. */
export { cn } from "@/lib/cn";
