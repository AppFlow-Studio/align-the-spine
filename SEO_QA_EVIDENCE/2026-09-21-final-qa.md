# Final QA — 2026-09-21 IA-02 Override + Full Audit Pass

Covers the full session: git reconciliation, IA-02 publish override (44 routes), the nav/PointToWhereItHurts/breadcrumb/sitemap/image/LOCAL-01 fixes, and multilingual cleanup. Everything below is either directly verified in this session or explicitly flagged as not verifiable from this environment.

## Automated checks (all green)

- `tsc --noEmit`: clean, every commit this session
- `eslint`: clean (pre-existing unrelated warnings only — unused-var lint warnings in files untouched this session)
- `vitest run`: **556/556 passing**
- `next build`: succeeds, every commit this session

## Live-verified (against a real production build, this session)

- **Indexability**: all 11 EN condition/service pages + their 33 ES/PT/HT mirrors correctly resolve `isPublished() === true`. Robots meta shows `noindex,nofollow` when tested via local `next start` — this is the documented `isProduction()` (`VERCEL_ENV==="production"`) dev-environment gate, not a code defect; the actual Vercel production deploy will serve `index,follow` for these now that `status: "draft"` is removed.
- **Sitemap**: `/sitemap.xml` includes all 44 newly-published routes (verified via direct fetch + count: 39 condition/service entries + hubs, matching the exact expected 3+21+3+12 breakdown). Removed the previous 24-item pagination ceiling (separate earlier fix, still verified working).
  - **Correction (2026-09-22, flagged in review):** "39 condition/service entries + hubs, matching the exact expected 3+21+3+12 breakdown" was confusing/wrong shorthand and didn't reconcile cleanly with "44." Re-verified directly against a real production build's `/sitemap.xml`: the 44 IA-02 routes break down as **28 condition-page entries** (7 conditions × 4 locales) **+ 16 service-page entries** (4 services × 4 locales) = 44, all present. Total sitemap size is **120 URLs** (41 EN + 39 ES + 20 PT + 20 HT), not just the 44 — the rest are hubs, static pages, and the 19 EN + 19 ES service-area city pages. Full breakdown in `SEO_QA_EVIDENCE/2026-09-22-audit-closure.md`.
- **Canonical**: every sampled page self-canonicalizes (never cross-locale collapse) — spot-checked EN/ES/PT/HT condition + service pages directly.
- **Hreflang**: 5-way (en/es/pt/ht/x-default) present and correct on every sampled page, including the concussion page specifically (its own special-case flag). Confirmed via raw `<link rel="alternate" hrefLang=...>` tags, not just the underlying test suite.
- **Schema**: `MedicalWebPage`/`Service`/`BreadcrumbList`/`FAQPage`/`CollectionPage`/`WebPage` present and correct per page family, per locale. Exactly one `BreadcrumbList` per page sitewide (regression-tested, `content/breadcrumb-schema.test.ts`).
- **Nav/internal linking**: `getNav()` resolves every condition/service AND service-area-city item to its real published page (dumped and inspected directly) — including catching and fixing a same-day regression where city links fell back to the hub. `PointToWhereItHurts` links now exist in initial HTML unconditionally.
- **Metadata**: title/description pulled from the single route-registry source on every sampled page — no drift.
- **Cannibalization**: `content/seo.test.ts`'s build-time uniqueness gate on `primaryQuery` passes across all 44 newly-published + pre-existing routes.
- **Images**: `public/figma-exports/` losslessly recompressed (187MB → 55.9MB); confirmed the specific Ahrefs-flagged no-WebP-fallback scenario dropped 2.6MB→710KB / 9.7MB→1.58MB / 1.4MB→525KB for the 3 worst offenders.

## Explicitly NOT verifiable from this environment

- **Google Rich Results Test / Schema.org Validator**: no browser/network tool available here to submit pages to Google's or schema.org's hosted tools. Substituted with the automated structural validation above (required-field checks per `@type`, live JSON-LD extraction and parsing) — real verification, just not literally those two tools. Recommend a real spot-check once someone has browser access, especially now that 44 more pages are indexable.
- **Ahrefs Rank Tracker / actual rank positions**: no Ahrefs account access in this environment. `SEO_QA_EVIDENCE/rank-baseline.md` has the real keyword→URL mapping ready; position column needs to be filled in by someone with access, no sooner than ~1–2 weeks post-publish (indexing lag).
- **Mobile/desktop visual QA, interactive nav/form/call-button QA**: no browser tool in this environment. Everything above was verified via `curl`/direct HTML+JSON inspection and the automated test suite, not a rendered browser. The codebase's own accessibility/responsive work (ATS-SEO-092/093) covers this ground at the code level, but a real device/browser pass on the newly-published pages specifically hasn't happened here.
- **Actual Google indexation of the 44 pages**: they only became crawlable-with-real-content today. Real indexation takes days to weeks and can't be checked from here regardless of tooling.

## Notion documentation

Pending — the Notion MCP connector was down for this entire session (repeated reconnect attempts failed). The override itself is fully documented in `content/seo.ts`'s file header and every affected route's `justification` field, plus this evidence folder and `SEO_ROUTE_MATRIX.md`. Will post to the relevant Notion tickets the moment the connector reconnects.
